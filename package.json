{
  "name": "file-encoder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --exec electron .",
    "start": "electron .",
    "build-win": "electron-packager . Шифратор --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Шифратор\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^4.0.5"
  },
  "devDependencies": {
    "electron-packager": "^13.1.0"
  }
}
